http://20.197.91.174 {
  encode zstd gzip

  # Minimal safe headers for HTTP (avoid HSTS on HTTP)
  header {
    X-Content-Type-Options "nosniff"
    Referrer-Policy "no-referrer-when-downgrade"
  }

  # Your Flask app inside Docker Compose is "web:8080"
  reverse_proxy web:8080
}


